"use client"; import { useState, useMemo } from "react"; import { useRouter } from "next/navigation"; import { Badge, SectionLabel, SearchBox, EmptyState, ConfirmDialog, Modal, AuthInput, SortButton, toast, InlineLoader } from "../../../components/shared"; import { C, STATUS_MAP, COLORS, daysLeft } from "../../../lib/constants"; import { useApp } from "../../../context/AppContext"; import AddressInput from "../../../components/AddressInput"; export default function PropertiesPage() { const { tenants, addTenant, updateTenant, deleteTenant, contracts, addContract, updateContract, loading, canUse, getPlanLimit } = useApp(); const [activeTab, setActiveTab] = useState("properties"); const [filter, setFilter] = useState("전체"); const [search, setSearch] = useState(""); const [sort, setSort] = useState({ key: "rent", dir: "desc" }); const [showModal, setShowModal] = useState(false); const [editTarget, setEditTarget] = useState(null); const [deleteTarget, setDeleteTarget] = useState(null); const [saving, setSaving] = useState(false); const [form, setForm] = useState({ pType: "주거", sub: "아파트", addr: "", rent: "", dep: "", name: "", phone: "", start: "", end: "", maintenance: "", pay_day: "5" }); const [showContractModal, setShowContractModal] = useState(false); const [contractForm, setContractForm] = useState({ tenant_id: "", start_date: "", end_date: "", rent: "", deposit: "", special_terms: "" }); const [contractEdit, setContractEdit] = useState(null); const resetForm = () => setForm({ pType: "주거", sub: "아파트", addr: "", rent: "", dep: "", name: "", phone: "", start: "", end: "", maintenance: "", pay_day: "5" }); const filtered = useMemo(() => { let list = [...tenants]; if (filter !== "전체") list = list.filter((t) => t.pType === filter); if (search) { const q = search.toLowerCase(); list = list.filter((t) => t.name?.toLowerCase().includes(q) || t.addr?.toLowerCase().includes(q) || t.sub?.toLowerCase().includes(q)); } list.sort((a, b) => { let va, vb; if (sort.key === "rent") { va = a.rent; vb = b.rent; } else if (sort.key === "dLeft") { va = daysLeft(a.end_date); vb = daysLeft(b.end_date); } else if (sort.key === "dep") { va = a.dep; vb = b.dep; } else { va = a.name; vb = b.name; } if (typeof va === "string") return sort.dir === "asc" ? va.localeCompare(vb) : vb.localeCompare(va); return sort.dir === "asc" ? va - vb : vb - va; }); return list; }, [tenants, filter, search, sort]); const toggleSort = (key) => setSort(sort.key === key ? { key, dir: sort.dir === "asc" ? "desc" : "asc" } : { key, dir: "desc" }); const openEdit = (t) => { setEditTarget(t); setForm({ pType: t.pType, sub: t.sub, addr: t.addr, rent: String(t.rent), dep: String(t.dep), name: t.name, phone: t.phone || "", start: t.start_date || "", end: t.end_date || "", maintenance: String(t.maintenance || ""), pay_day: String(t.pay_day || "5"), }); setShowModal(true); }; const saveTenant = async () => { if (!form.addr || !form.rent || !form.name) { toast("필수 항목을 입력하세요", "error"); return; } const payDay = parseInt(form.pay_day || "5"); if (isNaN(payDay) || payDay < 1 || payDay > 31) { toast("납부일은 1~31일 사이로 입력하세요", "error"); return; } setSaving(true); try { const color = COLORS[Math.floor(Math.random() * COLORS.length)]; const payload = { name: form.name, phone: form.phone || "", pType: form.pType, sub: form.sub, addr: form.addr, dep: Number(form.dep || 0), rent: Number(form.rent), start_date: form.start || null, end_date: form.end || "2026-12-31", status: "정상", color, intent: "미확인", maintenance: Number(form.maintenance || 0), pay_day: payDay, biz: null, contacts: [], }; if (editTarget) { await updateTenant(editTarget.id, payload); toast("물건 정보가 수정되었습니다"); } else { await addTenant(payload); toast("새 물건이 등록되었습니다"); } setShowModal(false); resetForm(); setEditTarget(null); } catch (e) { toast("저장 중 오류가 발생했습니다", "error"); console.error(e); } finally { setSaving(false); } }; const handleDelete = async () => { if (!deleteTarget) return; try { await deleteTenant(deleteTarget.id); toast(deleteTarget.name + "님 물건이 삭제되었습니다"); } catch { toast("삭제 중 오류가 발생했습니다", "error"); } setDeleteTarget(null); }; const getEnd = (t) => t.end_date || t.end || ""; return ( <div className="page-in page-padding" style={{ maxWidth: 920 }}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-end", marginBottom: 16, flexWrap: "wrap", gap: 12 }}> <div> <SectionLabel>PROPERTY MANAGEMENT</SectionLabel> <h1 style={{ fontSize: 24, fontWeight: 800, color: "#1a2744", letterSpacing: "-.4px" }}>물건 관리</h1> </div> <button onClick={() => { const limit = getPlanLimit("properties"); if (limit !== Infinity && tenants.length >= limit) { alert(`현재 플랜에서는 물건을 최대 ${limit}개까지 등록할 수 있어요.\n업그레이드하면 더 많이 등록할 수 있어요!`); window.location.href = "/dashboard/pricing"; return; } resetForm(); setEditTarget(null); setShowModal(true); }} className="btn-primary" style={{ padding: "10px 20px", borderRadius: 11, background: `linear-gradient(135deg,${C.indigo},${C.purple})`, border: "none", color: "#fff", fontWeight: 700, fontSize: 13, cursor: "pointer" }}> + 물건 추가 </button> </div> <div style={{ display: "flex", gap: 4, marginBottom: 18, borderBottom: "1px solid #ebe9e3", paddingBottom: 0 }}> {[ { key: "properties", label: "🏠 물건 목록", count: tenants.length }, { key: "contracts", label: "📝 계약서", count: contracts?.length || 0 }, ].map(tab => ( <button key={tab.key} onClick={() => setActiveTab(tab.key)} style={{ padding: "9px 18px", fontSize: 13, fontWeight: 700, cursor: "pointer", border: "none", background: "transparent", borderBottom: `2.5px solid ${activeTab === tab.key ? "#1a2744" : "transparent"}`, color: activeTab === tab.key ? "#1a2744" : "#8a8a9a", marginBottom: -1, transition: "all .15s" }}> {tab.label} <span style={{ fontSize: 11, background: activeTab === tab.key ? "#1a274415" : "#f0efe9", padding: "1px 6px", borderRadius: 10, marginLeft: 4 }}>{tab.count}</span> </button> ))} </div> {activeTab === "contracts" && ( <div> <div style={{ display: "flex", justifyContent: "flex-end", marginBottom: 14 }}> <button onClick={() => { setContractEdit(null); setContractForm({ tenant_id: "", start_date: "", end_date: "", rent: "", deposit: "", special_terms: "" }); setShowContractModal(true); }} style={{ padding: "9px 18px", borderRadius: 10, background: `linear-gradient(135deg,#1a2744,#5b4fcf)`, border: "none", color: "#fff", fontWeight: 700, fontSize: 13, cursor: "pointer" }}> + 계약서 작성 </button> </div> {!contracts?.length ? ( <EmptyState icon="📝" title="등록된 계약서가 없습니다" desc="계약서 작성 버튼으로 첫 계약서를 등록하세요" /> ) : ( <div style={{ background: "#fff", border: "1px solid #ebe9e3", borderRadius: 16, overflow: "hidden" }}> {contracts.map((c, i) => { const tenant = tenants.find(t => t.id === c.tenant_id); const dl = c.end_date ? Math.ceil((new Date(c.end_date) - new Date()) / 86400000) : null; return ( <div key={c.id} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "14px 20px", borderBottom: i < contracts.length - 1 ? "1px solid #f0efe9" : "none" }}> <div> <div style={{ display: "flex", gap: 8, alignItems: "center", marginBottom: 4 }}> <span style={{ fontSize: 14, fontWeight: 700, color: "#1a2744" }}>{tenant?.name || c.tenant_name || "알수없음"}</span> {dl !== null && <span style={{ fontSize: 10, fontWeight: 700, color: dl <= 90 ? "#e8445a" : "#8a8a9a", background: dl <= 90 ? "rgba(232,68,90,0.1)" : "#f0efe9", padding: "2px 8px", borderRadius: 20 }}>만료 D-{dl}</span>} </div> <p style={{ fontSize: 12, color: "#8a8a9a" }}> {c.start_date || "—"} ~ {c.end_date || "—"} &nbsp;·&nbsp; 월세 {c.rent || tenant?.rent || "—"}만원 &nbsp;·&nbsp; 보증금 {c.deposit || tenant?.dep || "—"}만원 </p> {c.special_terms && <p style={{ fontSize: 11, color: "#8a8a9a", marginTop: 3 }}>📌 {c.special_terms.slice(0, 60)}{c.special_terms.length > 60 ? "..." : ""}</p>} </div> <button onClick={() => { setContractEdit(c); setContractForm({ tenant_id: c.tenant_id || "", start_date: c.start_date || "", end_date: c.end_date || "", rent: c.rent || "", deposit: c.deposit || "", special_terms: c.special_terms || "" }); setShowContractModal(true); }} style={{ padding: "5px 12px", borderRadius: 8, fontSize: 11, fontWeight: 600, cursor: "pointer", border: "1px solid #ebe9e3", background: "transparent", color: "#1a2744" }}>수정</button> </div> ); })} </div> )} </div> )} <Modal open={showContractModal} onClose={() => setShowContractModal(false)}> <h2 style={{ fontSize: 18, fontWeight: 800, color: "#1a2744", marginBottom: 18 }}>{contractEdit ? "계약서 수정" : "계약서 작성"}</h2> <div style={{ display: "flex", flexDirection: "column", gap: 13 }}> <div> <p style={{ fontSize: 11, color: "#8a8a9a", fontWeight: 700, letterSpacing: ".5px", textTransform: "uppercase", marginBottom: 7 }}>세입자</p> <select value={contractForm.tenant_id} onChange={e => setContractForm(f => ({ ...f, tenant_id: e.target.value }))} style={{ width: "100%", padding: "10px 12px", borderRadius: 10, border: "1px solid #ebe9e3", fontSize: 13, color: "#1a2744", background: "#f8f7f4", outline: "none" }}> <option value="">선택하세요</option> {tenants.map(t => <option key={t.id} value={t.id}>{t.name} — {t.addr}</option>)} </select> </div> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}> <AuthInput label="계약 시작일" type="date" value={contractForm.start_date} onChange={e => setContractForm(f => ({ ...f, start_date: e.target.value }))} /> <AuthInput label="계약 종료일" type="date" value={contractForm.end_date} onChange={e => setContractForm(f => ({ ...f, end_date: e.target.value }))} /> </div> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}> <AuthInput label="월세 (만원)" type="number" placeholder="120" value={contractForm.rent} onChange={e => setContractForm(f => ({ ...f, rent: e.target.value }))} icon="💰" /> <AuthInput label="보증금 (만원)" type="number" placeholder="5000" value={contractForm.deposit} onChange={e => setContractForm(f => ({ ...f, deposit: e.target.value }))} icon="🏦" /> </div> <div> <p style={{ fontSize: 11, color: "#8a8a9a", fontWeight: 700, letterSpacing: ".5px", textTransform: "uppercase", marginBottom: 7 }}>특약 사항</p> <textarea value={contractForm.special_terms} onChange={e => setContractForm(f => ({ ...f, special_terms: e.target.value }))} placeholder="특약 사항을 입력하세요..." rows={3} style={{ width: "100%", padding: "11px 13px", fontSize: 13, color: "#1a2744", background: "#f8f7f4", border: "1px solid #ebe9e3", borderRadius: 10, resize: "vertical", outline: "none", fontFamily: "inherit" }} /> </div> <div style={{ display: "flex", gap: 10 }}> <button onClick={() => setShowContractModal(false)} style={{ flex: 1, padding: "12px", borderRadius: 11, background: "transparent", border: "1px solid #ebe9e3", color: "#8a8a9a", fontWeight: 600, fontSize: 13, cursor: "pointer" }}>취소</button> <button onClick={async () => { setSaving(true); try { const tenant = tenants.find(t => t.id === contractForm.tenant_id); const payload = { ...contractForm, rent: Number(contractForm.rent || 0), deposit: Number(contractForm.deposit || 0), tenant_name: tenant?.name || "" }; if (contractEdit) await updateContract(contractEdit.id, payload); else await addContract(payload); toast(contractEdit ? "계약서가 수정되었습니다" : "계약서가 등록되었습니다"); setShowContractModal(false); } catch { toast("저장 중 오류가 발생했습니다", "error"); } finally { setSaving(false); } }} disabled={saving} style={{ flex: 2, padding: "12px", borderRadius: 11, background: `linear-gradient(135deg,#1a2744,#5b4fcf)`, border: "none", color: "#fff", fontWeight: 700, fontSize: 13, cursor: "pointer", opacity: saving ? 0.7 : 1 }}> {saving ? "저장 중..." : contractEdit ? "수정 완료" : "저장하기"} </button> </div> </div> </Modal> {activeTab === "properties" && (<> <div style={{ display: "flex", gap: 10, marginBottom: 18, flexWrap: "wrap", alignItems: "center" }}> <div style={{ display: "flex", gap: 7 }}> {["전체", "주거", "상가", "토지"].map((f) => ( <button key={f} onClick={() => setFilter(f)} style={{ padding: "6px 16px", borderRadius: 18, fontSize: 12, fontWeight: 600, cursor: "pointer", border: `1px solid ${filter === f ? C.indigo : "#ebe9e3"}`, background: filter === f ? C.indigo + "20" : "transparent", color: filter === f ? C.indigo : C.muted }}>{f}</button> ))} </div> <div style={{ flex: 1, minWidth: 180 }}><SearchBox value={search} onChange={setSearch} placeholder="이름, 주소, 유형 검색..." /></div> <div style={{ display: "flex", gap: 5 }}> <SortButton label="월세" active={sort.key === "rent"} dir={sort.dir} onClick={() => toggleSort("rent")} /> <SortButton label="만료일" active={sort.key === "dLeft"} dir={sort.dir} onClick={() => toggleSort("dLeft")} /> <SortButton label="이름" active={sort.key === "name"} dir={sort.dir} onClick={() => toggleSort("name")} /> </div> </div> {loading ? <InlineLoader rows={4} /> : filtered.length === 0 ? ( <EmptyState icon="🏠" title={search ? "검색 결과가 없습니다" : "등록된 물건이 없습니다"} desc={search ? "다른 키워드로 검색해보세요" : "첫 물건을 추가해보세요"} action={!search ? "+ 물건 추가" : null} onAction={() => { resetForm(); setShowModal(true); }} /> ) : ( <div style={{ display: "flex", flexDirection: "column", gap: 9 }}> {filtered.map((t) => { const dl = daysLeft(getEnd(t)); return ( <div key={t.id} className="hover-lift" style={{ background: "#ffffff", border: "1px solid #ebe9e3", borderRadius: 14, padding: "17px 20px" }}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 12 }}> <div style={{ display: "flex", gap: 13, alignItems: "center", flex: 1, minWidth: 0 }}> <div style={{ width: 42, height: 42, borderRadius: 12, background: (t.color || t.c || C.indigo) + "20", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 19, flexShrink: 0 }}> {t.pType === "상가" ? "🏪" : t.pType === "토지" ? "🌱" : "🏠"} </div> <div style={{ minWidth: 0 }}> <div style={{ display: "flex", gap: 7, marginBottom: 5, flexWrap: "wrap" }}> <span style={{ fontSize: 10, fontWeight: 700, color: t.pType === "상가" ? C.amber : t.pType === "토지" ? "#0d9488" : C.indigo, background: t.pType === "상가" ? C.amber + "18" : t.pType === "토지" ? "#0d948818" : C.indigo + "18", padding: "2px 7px", borderRadius: 5 }}>{t.sub}</span> <Badge label={t.status} map={STATUS_MAP} /> </div> <p style={{ fontSize: 14, fontWeight: 700, color: "#1a2744", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", maxWidth: "100%" }}>서울 {t.addr}</p> <p style={{ fontSize: 11, color: "#8a8a9a", marginTop: 2 }}>👤 {t.name} · {t.phone}{t.pay_day ? ` · 매월 ${t.pay_day}일 납부` : ""}</p> </div> </div> <div style={{ display: "flex", alignItems: "center", gap: 16 }}> <div style={{ textAlign: "right" }}> <p style={{ fontSize: 18, fontWeight: 800, color: "#1a2744" }}>{t.rent}만원<span style={{ fontSize: 11, color: "#8a8a9a", fontWeight: 400 }}>/월</span></p> <p style={{ fontSize: 11, color: "#8a8a9a", marginTop: 2 }}>보증금 {(t.dep / 10000).toFixed(1)}억</p> <p style={{ fontSize: 11, color: dl <= 60 ? C.amber : C.muted, marginTop: 3, fontWeight: 600 }}>만료 D-{dl}</p> </div> <div style={{ display: "flex", flexDirection: "column", gap: 4 }}> <button onClick={() => openEdit(t)} style={{ padding: "5px 10px", borderRadius: 7, fontSize: 11, fontWeight: 600, cursor: "pointer", border: "1px solid #ebe9e3", background: "transparent", color: "#1a2744" }}>수정</button> <button onClick={() => setDeleteTarget(t)} style={{ padding: "5px 10px", borderRadius: 7, fontSize: 11, fontWeight: 600, cursor: "pointer", border: `1px solid ${C.rose}33`, background: "transparent", color: "#e8445a" }}>삭제</button> </div> </div> </div> </div> ); })} </div> )} <Modal open={showModal} onClose={() => { setShowModal(false); resetForm(); }}> <div style={{ marginBottom: 20 }}> <h2 style={{ fontSize: 19, fontWeight: 800, color: "#1a2744" }}>{editTarget ? "물건 수정" : "물건 추가"}</h2> <p style={{ fontSize: 13, color: "#8a8a9a", marginTop: 4 }}>{editTarget ? "물건 정보를 수정합니다" : "새 임대 물건 정보를 입력하세요"}</p> </div> <div style={{ display: "flex", flexDirection: "column", gap: 14 }}> <div style={{ display: "flex", gap: 10 }}> {[ { type: "주거", icon: "🏠", defaultSub: "아파트" }, { type: "상가", icon: "🏪", defaultSub: "1층 상가" }, { type: "토지", icon: "🌱", defaultSub: "전·답" }, ].map(({ type, icon, defaultSub }) => ( <button key={type} onClick={() => setForm((f) => ({ ...f, pType: type, sub: defaultSub }))} style={{ flex: 1, padding: "11px", borderRadius: 10, border: `2px solid ${form.pType === type ? (type === "상가" ? C.amber : type === "토지" ? "#0d9488" : C.indigo) : "#ebe9e3"}`, background: form.pType === type ? (type === "상가" ? C.amber : type === "토지" ? "#0d9488" : C.indigo) + "18" : "transparent", color: form.pType === type ? (type === "상가" ? C.amber : type === "토지" ? "#0d9488" : C.indigo) : C.muted, fontWeight: 700, fontSize: 13, cursor: "pointer" }}> {icon} {type} </button> ))} </div> <div> <p style={{ fontSize: 11, fontWeight: 700, color: "#8a8a9a", marginBottom: 8, textTransform: "uppercase", letterSpacing: "0.5px" }}>세부 유형</p> <div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}> {(form.pType === "주거" ? ["아파트", "빌라", "오피스텔", "단독주택", "다세대"] : form.pType === "상가" ? ["1층 상가", "2층 이상", "지하", "사무실", "창고·물류"] : ["전·답", "임야", "대지", "잡종지", "기타 토지"] ).map((s) => ( <button key={s} onClick={() => setForm((f) => ({ ...f, sub: s }))} style={{ padding: "6px 12px", borderRadius: 8, fontSize: 12, fontWeight: 700, cursor: "pointer", border: "none", background: form.sub === s ? (form.pType === "상가" ? C.amber : form.pType === "토지" ? "#0d9488" : C.indigo) : "#f0efe9", color: form.sub === s ? "#fff" : "#8a8a9a", transition: "all .15s" }}> {s} </button> ))} </div> </div> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}> <AuthInput label="세입자 이름 *" placeholder="홍길동" value={form.name} onChange={(e) => setForm((f) => ({ ...f, name: e.target.value }))} icon="👤" /> <AuthInput label="연락처" placeholder="010-0000-0000" value={form.phone} onChange={(e) => setForm((f) => ({ ...f, phone: e.target.value }))} icon="📞" /> </div> <AddressInput label="주소" value={form.addr} onChange={(v) => setForm((f) => ({ ...f, addr: v }))} onSelect={(v) => setForm((f) => ({ ...f, addr: v }))} placeholder="마포구 합정동 123" /> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}> <AuthInput label={form.pType === "토지" ? "토지 가액 (만원)" : "보증금 (만원)"} placeholder={form.pType === "토지" ? "100000" : "50000"} value={form.dep} onChange={(e) => setForm((f) => ({ ...f, dep: e.target.value }))} icon="💵" /> <AuthInput label={form.pType === "토지" ? "월 임대료 (만원)" : "월세 (만원) *"} placeholder={form.pType === "토지" ? "50" : "120"} value={form.rent} onChange={(e) => setForm((f) => ({ ...f, rent: e.target.value }))} icon="💰" /> </div> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}> <AuthInput label="계약 시작일" type="date" value={form.start} onChange={(e) => setForm((f) => ({ ...f, start: e.target.value }))} /> <AuthInput label="계약 만료일" type="date" value={form.end} onChange={(e) => setForm((f) => ({ ...f, end: e.target.value }))} /> </div> <div style={{ background: "rgba(26,39,68,0.03)", border: "1px solid rgba(26,39,68,0.1)", borderRadius: 12, padding: "14px 16px" }}> <p style={{ fontSize: 11, fontWeight: 800, color: "#1a2744", marginBottom: 10, textTransform: "uppercase", letterSpacing: "0.5px" }}>📅 납부일 설정</p> <div style={{ display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 10 }}> {[1, 5, 10, 15, 20, 25].map(d => ( <button key={d} onClick={() => setForm(f => ({ ...f, pay_day: String(d) }))} style={{ padding: "6px 14px", borderRadius: 20, fontSize: 13, fontWeight: 700, cursor: "pointer", border: `1.5px solid ${form.pay_day === String(d) ? C.indigo : "#ebe9e3"}`, background: form.pay_day === String(d) ? C.indigo : "transparent", color: form.pay_day === String(d) ? "#fff" : "#8a8a9a", transition: "all .15s" }}> {d}일 </button> ))} </div> <div style={{ display: "flex", alignItems: "center", gap: 8 }}> <span style={{ fontSize: 12, color: "#8a8a9a" }}>직접 입력:</span> <input type="number" min={1} max={31} value={form.pay_day} onChange={e => setForm(f => ({ ...f, pay_day: e.target.value }))} style={{ width: 70, padding: "6px 10px", borderRadius: 8, border: "1.5px solid #ebe9e3", fontSize: 13, fontWeight: 700, color: "#1a2744", textAlign: "center", outline: "none" }} /> <span style={{ fontSize: 12, color: "#8a8a9a" }}>일 (매월)</span> </div> <p style={{ fontSize: 11, color: "#8a8a9a", marginTop: 8 }}>카카오 납부일 예정 알림이 이 납부일 기준 D-3에 자동 발송됩니다.</p> </div> {(form.pType === "상가" || form.sub === "오피스텔") && ( <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10 }}> <AuthInput label="관리비 (만원/월)" placeholder="예: 15" value={form.maintenance} onChange={(e) => setForm((f) => ({ ...f, maintenance: e.target.value }))} icon="🏢" /> <div style={{ background: "rgba(15,165,115,0.06)", border: "1px solid rgba(15,165,115,0.2)", borderRadius: 10, padding: "10px 13px", display: "flex", alignItems: "center" }}> <div> <p style={{ fontSize: 11, fontWeight: 700, color: "#0fa573", marginBottom: 3 }}>총 월 수익</p> <p style={{ fontSize: 13, fontWeight: 800, color: "#1a2744" }}> {form.rent && form.maintenance ? `월 ${(Number(form.rent) + Number(form.maintenance)).toLocaleString()}만원` : form.rent ? `월 ${Number(form.rent).toLocaleString()}만원` : "—"} </p> <p style={{ fontSize: 10, color: "#8a8a9a", marginTop: 2 }}>월세 + 관리비</p> </div> </div> </div> )} <div style={{ display: "flex", gap: 10, marginTop: 6 }}> <button onClick={() => { setShowModal(false); resetForm(); }} style={{ flex: 1, padding: "12px", borderRadius: 11, background: "transparent", border: "1px solid #ebe9e3", color: "#8a8a9a", fontWeight: 600, fontSize: 13, cursor: "pointer" }}>취소</button> <button onClick={saveTenant} disabled={saving} className="btn-primary" style={{ flex: 2, padding: "12px", borderRadius: 11, background: `linear-gradient(135deg,${C.indigo},${C.purple})`, border: "none", color: "#fff", fontWeight: 700, fontSize: 13, cursor: "pointer", opacity: saving ? 0.7 : 1 }}> {saving ? "저장 중..." : editTarget ? "저장하기" : "등록하기"} </button> </div> </div> </Modal> <ConfirmDialog open={!!deleteTarget} title="물건 삭제" desc={deleteTarget ? `${deleteTarget.name}님의 ${deleteTarget.addr} 물건을 삭제하시겠습니까?` : ""} onConfirm={handleDelete} onCancel={() => setDeleteTarget(null)} danger /> </>)} <div style={{ marginTop: 20, background: "linear-gradient(135deg,rgba(59,91,219,0.05),rgba(91,79,207,0.03))", border: "1px solid rgba(59,91,219,0.15)", borderRadius: 16, padding: "16px 20px" }}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 12 }}> <div style={{ display: "flex", gap: 12, alignItems: "center" }}> <span style={{ fontSize: 28 }}>🧾</span> <div> <p style={{ fontSize: 13, fontWeight: 800, color: "#1a2744", marginBottom: 2 }}>임대 소득세 신고 · 절세 전략이 필요하신가요?</p> <p style={{ fontSize: 11, color: "#8a8a9a" }}>제휴 세무사가 임대인 전문 세무 서비스를 제공합니다 · 첫 상담 무료</p> </div> </div> <button onClick={() => alert("🚧 세무사 연결 서비스 준비 중입니다.\n빠른 시일 내에 오픈할게요!")} style={{ padding: "9px 18px", borderRadius: 10, background: "linear-gradient(135deg,#3b5bdb,#5b4fcf)", border: "none", color: "#fff", fontSize: 12, fontWeight: 700, cursor: "pointer", flexShrink: 0 }}> 세무사 연결 → </button> </div> </div> </div> ); }