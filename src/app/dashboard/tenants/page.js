"use client"; import { useState, useMemo } from "react"; import { useRouter } from "next/navigation"; import { Badge, SectionLabel, SearchBox, EmptyState, Modal, toast, SkeletonTable } from "../../../components/shared"; import { C, STATUS_MAP, INTENT_MAP, daysLeft } from "../../../lib/constants"; import { useApp } from "../../../context/AppContext"; export default function TenantsPage() { const router = useRouter(); const { tenants, updateTenantContacts, updateTenantIntent, loading } = useApp(); if (loading) return <SkeletonTable rows={6} cols={3} />; const [selected, setSelected] = useState(null); const [filter, setFilter] = useState("전체"); const [search, setSearch] = useState(""); const [showContact, setShowContact] = useState(false); const [contactNote, setContactNote] = useState({ type: "납부확인", note: "" }); const [saving, setSaving] = useState(false); const getEnd = (t) => t.end_date || t.end || ""; const filtered = useMemo(() => { let list = [...tenants]; if (filter === "만료임박") list = list.filter((t) => daysLeft(getEnd(t)) <= 90); else if (filter === "미납") list = list.filter((t) => t.status === "미납"); else if (filter !== "전체") list = list.filter((t) => t.pType === filter); if (search) { const q = search.toLowerCase(); list = list.filter((t) => t.name?.toLowerCase().includes(q) || t.addr?.toLowerCase().includes(q)); } return list; }, [tenants, filter, search]); const sel = selected ? tenants.find((t) => t.id === selected.id) : null; const addContact = async () => { if (!sel || !contactNote.note.trim()) { toast("내용을 입력하세요", "error"); return; } setSaving(true); try { const newContact = { date: new Date().toISOString().slice(0, 10), type: contactNote.type, note: contactNote.note }; const updatedContacts = [newContact, ...(sel.contacts || [])]; await updateTenantContacts(sel.id, updatedContacts); toast("연락 기록이 저장되었습니다"); setShowContact(false); setContactNote({ type: "납부확인", note: "" }); } catch { toast("저장 중 오류가 발생했습니다", "error"); } finally { setSaving(false); } }; const handleUpdateIntent = async (intent) => { if (!sel) return; try { await updateTenantIntent(sel.id, intent); toast("갱신 의향이 변경되었습니다"); } catch { toast("변경 중 오류가 발생했습니다", "error"); } }; return ( <div className="page-in tenant-split" style={{ display: "flex", height: "100%", minHeight: "calc(100vh - 0px)" }}> {/* 왼쪽 목록 */} <div className="tenant-list-panel" style={{ width: 310, borderRight: "1px solid #ebe9e3", display: "flex", flexDirection: "column", flexShrink: 0 }}> <div style={{ padding: "26px 18px 14px", borderBottom: "1px solid var(--border)" }}> <SectionLabel>TENANT MANAGEMENT</SectionLabel> <h1 style={{ fontSize: 20, fontWeight: 800, color: "var(--text)" }}>세입자 관리</h1> <div style={{ marginTop: 10 }}><SearchBox value={search} onChange={setSearch} placeholder="이름, 주소 검색..." /></div> </div> <div style={{ padding: "10px 12px", borderBottom: "1px solid var(--border)", display: "flex", gap: 5, flexWrap: "wrap" }}> {["전체", "주거", "상가", "토지", "만료임박", "미납"].map((f) => ( <button key={f} onClick={() => setFilter(f)} style={{ padding: "4px 10px", borderRadius: 13, fontSize: 11, fontWeight: 600, cursor: "pointer", border: `1px solid ${filter === f ? C.indigo : "var(--border)"}`, background: filter === f ? C.indigo + "20" : "transparent", color: filter === f ? C.indigo : C.muted, minHeight: 30 }}>{f}</button> ))} </div> <div style={{ flex: 1, overflowY: "auto" }}> {filtered.length === 0 ? ( <div style={{ padding: 24, textAlign: "center" }}> <div style={{ fontSize: 32, marginBottom: 10 }}>{search ? "🔍" : tenants.length === 0 ? "🏠" : "👤"}</div> <p style={{ fontSize: 13, fontWeight: 700, color: "#1a2744", marginBottom: 6 }}> {search ? "검색 결과 없음" : tenants.length === 0 ? "등록된 물건이 없습니다" : "해당 조건의 세입자 없음"} </p> <p style={{ fontSize: 11, color: "#8a8a9a", marginBottom: tenants.length === 0 ? 16 : 0 }}> {search ? "다른 키워드로 검색해보세요" : tenants.length === 0 ? "먼저 물건을 등록하고\n세입자를 연결해보세요" : ""} </p> {tenants.length === 0 && ( <button onClick={() => router.push("/dashboard/properties")} style={{ padding: "8px 18px", borderRadius: 10, background: "#1a2744", color: "#fff", border: "none", fontSize: 12, fontWeight: 700, cursor: "pointer" }}> 물건 등록하러 가기 → </button> )} </div> ) : filtered.map((t) => { const isSelected = sel?.id === t.id; const dl = daysLeft(getEnd(t)); return ( <div key={t.id} onClick={() => setSelected(t)} style={{ padding: "13px 16px", borderBottom: "1px solid #ebe9e3", cursor: "pointer", borderLeft: "3px solid " + (isSelected ? C.indigo : "transparent"), background: isSelected ? "#f8f7f4" : "transparent", transition: "all .15s" }} onMouseEnter={(e) => { if (!isSelected) e.currentTarget.style.background = "#f5f4f0"; }} onMouseLeave={(e) => { if (!isSelected) e.currentTarget.style.background = "transparent"; }}> <div style={{ display: "flex", gap: 9, alignItems: "flex-start" }}> <div style={{ width: 34, height: 34, borderRadius: 10, background: (t.color || C.indigo) + "20", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 13, fontWeight: 800, color: t.color || C.indigo, flexShrink: 0 }}>{t.name?.[0]}</div> <div style={{ flex: 1, minWidth: 0 }}> <div style={{ display: "flex", justifyContent: "space-between" }}> <p style={{ fontSize: 13, fontWeight: 700, color: "#1a2744" }}>{t.name}</p> <Badge label={t.status} map={STATUS_MAP} /> </div> <p style={{ fontSize: 11, color: "#8a8a9a", marginTop: 2, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{t.sub} · {t.addr}</p> <div style={{ display: "flex", gap: 8, marginTop: 5 }}> <span style={{ fontSize: 12, fontWeight: 700, color: "#1a2744" }}>{Number(t.rent).toLocaleString()}만원/월</span> <span style={{ fontSize: 12, color: dl <= 60 ? C.amber : C.muted }}>D-{dl}</span> </div> </div> </div> </div> ); })} </div> </div> {/* 오른쪽 상세 */} {!sel ? ( <div style={{ flex: 1, display: "flex", alignItems: "center", justifyContent: "center" }}> <EmptyState icon="👤" title="세입자를 선택하세요" desc="왼쪽 목록에서 세입자를 클릭하면 상세 정보를 볼 수 있습니다" /> </div> ) : ( <div style={{ flex: 1, padding: "26px 30px", overflowY: "auto" }}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: 22, flexWrap: "wrap", gap: 12 }}> <div style={{ display: "flex", gap: 12, alignItems: "center" }}> <div style={{ width: 46, height: 46, borderRadius: 13, background: (sel.color || C.indigo) + "20", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 19, fontWeight: 800, color: sel.color || C.indigo }}>{sel.name?.[0]}</div> <div> <div style={{ display: "flex", gap: 7, alignItems: "center", marginBottom: 4, flexWrap: "wrap" }}> <h2 style={{ fontSize: 17, fontWeight: 800, color: "#1a2744" }}>{sel.name}</h2> <Badge label={sel.status} map={STATUS_MAP} /> <Badge label={sel.intent} map={INTENT_MAP} /> </div> <p style={{ fontSize: 12, color: "#8a8a9a" }}>{sel.sub} · {sel.addr} · {sel.phone ? <a href={`tel:${sel.phone}`} style={{ color: "inherit", textDecoration: "none", fontWeight: 700 }} onClick={e => e.stopPropagation()}>{sel.phone} 📞</a> : "-"}</p> </div> </div> <div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}> <button onClick={() => setShowContact(true)} style={{ padding: "7px 13px", borderRadius: 8, background: C.indigo + "18", border: `1px solid ${C.indigo}40`, color: "#1a2744", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>+ 연락 기록</button> {sel.phone && <a href={`tel:${sel.phone}`} style={{ padding: "7px 13px", borderRadius: 8, background: "rgba(15,165,115,0.1)", border: "1px solid rgba(15,165,115,0.3)", color: "#0fa573", fontSize: 12, fontWeight: 700, cursor: "pointer", textDecoration: "none", display: "inline-flex", alignItems: "center", gap: 4 }}>📞 전화</a>} <button onClick={() => router.push("/dashboard/certified")} style={{ padding: "7px 13px", borderRadius: 8, background: C.rose + "18", border: `1px solid ${C.rose}40`, color: "#e8445a", fontSize: 12, fontWeight: 700, cursor: "pointer" }}>📨 내용증명</button> {daysLeft(getEnd(sel)) <= 90 && ( <select onChange={(e) => { if (e.target.value) { handleUpdateIntent(e.target.value); e.target.value = ""; } }} defaultValue="" style={{ padding: "7px 13px", borderRadius: 8, background: C.amber + "18", border: `1px solid ${C.amber}40`, color: "#e8960a", fontSize: 12, fontWeight: 700, cursor: "pointer", appearance: "none" }}> <option value="" disabled>갱신 의향 변경</option> {["갱신의향 있음", "협의중", "갱신의향 없음", "미확인"].map((v) => <option key={v} value={v}>{v}</option>)} </select> )} </div> </div> {sel.status === "미납" && ( <div style={{ background: "rgba(232,68,90,0.06)", border: "1px solid rgba(232,68,90,0.2)", borderRadius: 12, padding: "12px 16px", marginBottom: 16, display: "flex", justifyContent: "space-between", alignItems: "center", gap: 12 }}> <div> <p style={{ fontSize: 12, fontWeight: 800, color: "#e8445a", marginBottom: 2 }}>⚠️ 미납 세입자</p> <p style={{ fontSize: 11, color: "#8a8a9a" }}>수금 현황 확인 후 내용증명 발송을 권장합니다</p> </div> <div style={{ display: "flex", gap: 6, flexShrink: 0 }}> <button onClick={() => router.push("/dashboard/payments")} style={{ padding: "6px 11px", borderRadius: 8, background: "transparent", border: "1px solid #e8445a50", color: "#e8445a", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>수금확인</button> <button onClick={() => router.push("/dashboard/certified")} style={{ padding: "6px 11px", borderRadius: 8, background: "#e8445a", border: "none", color: "#fff", fontSize: 11, fontWeight: 700, cursor: "pointer" }}>내용증명 →</button> </div> </div> )} {sel.status !== "미납" && daysLeft(getEnd(sel)) <= 90 && ( <div style={{ background: "rgba(232,150,10,0.06)", border: "1px solid rgba(232,150,10,0.2)", borderRadius: 12, padding: "12px 16px", marginBottom: 16, display: "flex", justifyContent: "space-between", alignItems: "center", gap: 12 }}> <div> <p style={{ fontSize: 12, fontWeight: 800, color: "#e8960a", marginBottom: 2 }}>📅 계약 만료 {daysLeft(getEnd(sel))}일 전</p> <p style={{ fontSize: 11, color: "#8a8a9a" }}>갱신 또는 퇴거 여부를 조속히 확인하세요</p> </div> <button onClick={() => router.push("/dashboard/contracts")} style={{ padding: "6px 11px", borderRadius: 8, background: "#e8960a", border: "none", color: "#fff", fontSize: 11, fontWeight: 700, cursor: "pointer", flexShrink: 0 }}>계약서 확인 →</button> </div> )} <div className="detail-2col" style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 14 }}> <div style={{ background: "#ffffff", border: "1px solid #ebe9e3", borderRadius: 13, padding: "18px" }}> <SectionLabel>CONTRACT</SectionLabel> {[ ["보증금", (sel.dep / 10000).toFixed(1) + "억원", undefined], ["월세", Number(sel.rent).toLocaleString() + "만원", C.emerald], ["만료일", getEnd(sel), daysLeft(getEnd(sel)) <= 90 ? C.amber : undefined], ["잔여일", "D-" + daysLeft(getEnd(sel)), daysLeft(getEnd(sel)) <= 60 ? C.rose : C.emerald], ].map(([l, v, a]) => ( <div key={l} style={{ display: "flex", justifyContent: "space-between", padding: "8px 0", borderBottom: "1px solid #ebe9e3" }}> <span style={{ fontSize: 12, color: "#8a8a9a" }}>{l}</span> <span style={{ fontSize: 13, fontWeight: 600, color: a || C.text }}>{v}</span> </div> ))} </div> <div style={{ background: "#ffffff", border: "1px solid #ebe9e3", borderRadius: 13, padding: "18px" }}> <SectionLabel>RECENT CONTACTS</SectionLabel> {!sel.contacts || sel.contacts.length === 0 ? ( <EmptyState icon="📝" title="연락 기록 없음" desc="연락 기록을 추가해보세요" /> ) : sel.contacts.slice(0, 5).map((ct, i) => ( <div key={i} style={{ padding: "9px 0", borderBottom: i < Math.min(sel.contacts.length, 5) - 1 ? "1px solid #ebe9e3" : "none" }}> <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 3 }}> <span style={{ fontSize: 10, fontWeight: 700, color: "#1a2744", background: C.indigo + "18", padding: "2px 7px", borderRadius: 5 }}>{ct.type}</span> <span style={{ fontSize: 10, color: "#8a8a9a" }}>{ct.date}</span> </div> <p style={{ fontSize: 12, color: "#1a2744", lineHeight: 1.6 }}>{ct.note}</p> </div> ))} </div> </div> </div> )} <Modal open={showContact} onClose={() => setShowContact(false)} width={420}> <h3 style={{ fontSize: 16, fontWeight: 800, color: "#1a2744", marginBottom: 16 }}>연락 기록 추가</h3> <div style={{ display: "flex", flexDirection: "column", gap: 13 }}> <div> <p style={{ fontSize: 11, color: "#8a8a9a", fontWeight: 700, letterSpacing: ".5px", textTransform: "uppercase", marginBottom: 7 }}>연락 유형</p> <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}> {["납부확인", "수리요청", "갱신협의", "미납독촉", "기타"].map((t) => ( <button key={t} onClick={() => setContactNote((n) => ({ ...n, type: t }))} style={{ padding: "5px 11px", borderRadius: 8, fontSize: 12, fontWeight: 600, cursor: "pointer", border: `1px solid ${contactNote.type === t ? C.indigo : "#ebe9e3"}`, background: contactNote.type === t ? C.indigo + "20" : "transparent", color: contactNote.type === t ? C.indigo : C.muted }}>{t}</button> ))} </div> </div> <div> <p style={{ fontSize: 11, color: "#8a8a9a", fontWeight: 700, letterSpacing: ".5px", textTransform: "uppercase", marginBottom: 7 }}>내용</p> <textarea value={contactNote.note} onChange={(e) => setContactNote((n) => ({ ...n, note: e.target.value }))} placeholder="연락 내용을 입력하세요..." rows={3} style={{ width: "100%", padding: "11px 13px", fontSize: 13, color: "#1a2744", background: "#f8f7f4", border: "1px solid #ebe9e3", borderRadius: 10, resize: "vertical", outline: "none" }} /> </div> <div style={{ display: "flex", gap: 9 }}> <button onClick={() => setShowContact(false)} style={{ flex: 1, padding: "11px", borderRadius: 10, background: "transparent", border: "1px solid #ebe9e3", color: "#8a8a9a", fontWeight: 600, fontSize: 13, cursor: "pointer" }}>취소</button> <button onClick={addContact} disabled={saving} className="btn-primary" style={{ flex: 2, padding: "11px", borderRadius: 10, background: `linear-gradient(135deg,${C.indigo},${C.purple})`, border: "none", color: "#fff", fontWeight: 700, fontSize: 13, cursor: "pointer" }}> {saving ? "저장 중..." : "저장하기"} </button> </div> </div> </Modal> <div style={{ marginTop: 20, background: "linear-gradient(135deg,rgba(59,91,219,0.05),rgba(91,79,207,0.03))", border: "1px solid rgba(59,91,219,0.15)", borderRadius: 16, padding: "16px 20px" }}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 12 }}> <div style={{ display: "flex", gap: 12, alignItems: "center" }}> <span style={{ fontSize: 28 }}>🧾</span> <div> <p style={{ fontSize: 13, fontWeight: 800, color: "#1a2744", marginBottom: 2 }}>임대 소득세 신고 · 절세 전략이 필요하신가요?</p> <p style={{ fontSize: 11, color: "#8a8a9a" }}>제휴 세무사가 임대인 전문 세무 서비스를 제공합니다 · 첫 상담 무료</p> </div> </div> <button onClick={() => alert("🚧 세무사 연결 서비스 준비 중입니다.\n빠른 시일 내에 오픈할게요!")} style={{ padding: "9px 18px", borderRadius: 10, background: "linear-gradient(135deg,#3b5bdb,#5b4fcf)", border: "none", color: "#fff", fontSize: 12, fontWeight: 700, cursor: "pointer", flexShrink: 0 }}> 세무사 연결 → </button> </div> </div> </div> ); }
